import type { LocatorObj } from '../types';

export const cartPageLocators: LocatorObj = {
  // CSS Selector Locators
  cartRows: '#cart_info tbody tr',
  cartDescriptionLink: '.cart_description h4 a',
};
