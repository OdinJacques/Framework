import type { LocatorObj } from '../types';

export const homePageLocators: LocatorObj = {
  // CSS Selector Locators
  featuredProductCards: '.features_items .product-image-wrapper',
  productInfoText: '.productinfo p',
};
